// dados simples: numericos, strings, boolean..
// array: [1,2,3, 'ana']
// objetos: {nome: 'gabriel' , idade: 24}
// funções: function somar(){}, () => {}

//objetos literais
{
    // propriedade valor
    // key : velue
}

perfil.roupas = ['camisetas','shots', 'jeans']

const perfil = {
    nome: 'gabriel'
    idade: 24,
    profissao: "caixa"
    solteiro: true,

}

console.log(objetos.entries(perfil)) //traz chave e valor
console.log(objetos.keys(perfil)) //traz chave 
console.log(objetos.values(perfil)) //traz  valor


// let dadosperfil = []

// for(let dados in perfil){
//     dadosperfil.push(perfil[dado])
// }
// alert(dadosperfil)

// console.log(perfil.nome)